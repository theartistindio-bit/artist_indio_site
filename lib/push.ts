// Minimal stub. Replace with your existing push broadcast if available.
export async function sendPushToAll(_payload: { title: string; body: string; url?: string }) {
  // no-op in this starter. Integrate your push subscription store to broadcast.
  return true;
}
